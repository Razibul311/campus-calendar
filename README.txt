BD STUDENT CALENDAR - CAPACITOR

1. Open this folder in VS Code.
2. Open Terminal in the project root.
3. Run:
   npm install
   npx cap add android
   npx cap sync
   npx cap open android

No Flask/Python server is required.

The current version stores events locally using IndexedDB.
